pipeline {
    agent node {
        label 'maven'
    }

    stages {
        stage('clone the project on build server') {
            steps {
                git branch: 'main', url: 'https://github.com/Ranjeetd26/tweet-trend-new.git'
            }
        }
    }
}
