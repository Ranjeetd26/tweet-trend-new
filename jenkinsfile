pipeline {
    agent any

    environment {
        PATH = "/opt/apache-maven-3.9.11/bin:$PATH"
    }

    stages {
        stage('build-stage') {
            steps {
                sh 'mvn clean deploy'
            }
        }
    }
}
